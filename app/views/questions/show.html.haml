%b= @question.title

%p=@question.body.html_safe

- if current_user = @question.user
  = link_to 'Delete question', @question, method: :delete, data: { confirm: 'Are you sure?' }

Count of questions:
= @question.answers.count

- @question.answers.each_with_index do |answer, index|
  .panel.panel-primary
    .panel-heading= "question #{index + 1}"
    .panel-body
      = answer.body.html_safe
      - if current_user == answer.user
        = link_to 'Delete', question_answer_path(@question), method: :delete, data: { confirm: 'Are you sure?' }

= form_for @answer, url: question_answers_path(@question) do |f|
  = f.label :body, 'Answer'
  = f.text_field :body
  = f.submit 'Create answer'

= link_to 'Questions', questions_path, class: 'btn btn-default'
